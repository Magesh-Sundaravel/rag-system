# IRSA: pods that call AWS (S3, SQS) assume this role. Annotation filled per-account.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: rag-aws
  namespace: rag-prod
  annotations:
    eks.amazonaws.com/role-arn: ${AWS_IRSA_ROLE_ARN}
